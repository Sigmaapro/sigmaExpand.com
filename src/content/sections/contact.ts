// This file is safe to edit for content updates (no code knowledge needed)

import { siteRestByLang } from "../site";
import { localizeFromLangRecord } from "../_contentLocales";

export const contactSectionContent = localizeFromLangRecord({
  EN: {
    contact: siteRestByLang.EN.contact,
    stayConnected: siteRestByLang.EN.stayConnected,
  },
  FA: {
    contact: siteRestByLang.FA.contact,
    stayConnected: siteRestByLang.FA.stayConnected,
  },
  TR: {
    contact: siteRestByLang.TR.contact,
    stayConnected: siteRestByLang.TR.stayConnected,
  },
  ZH: {
    contact: siteRestByLang.ZH.contact,
    stayConnected: siteRestByLang.ZH.stayConnected,
  },
  ES: {
    contact: siteRestByLang.ES.contact,
    stayConnected: siteRestByLang.ES.stayConnected,
  },
  RU: {
    contact: siteRestByLang.RU.contact,
    stayConnected: siteRestByLang.RU.stayConnected,
  },
  AR: {
    contact: siteRestByLang.AR.contact,
    stayConnected: siteRestByLang.AR.stayConnected,
  },
});
